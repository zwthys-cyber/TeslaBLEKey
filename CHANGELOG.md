# 版本记录

本文件记录已发布版本的重要用户可见变化。完整提交历史以 Git 为准。

## 2.2.1

- 修复“主动点击解锁正常，但靠近拉门无法解锁”：Phone Key 现在持续监听车辆在拉动门把手时发送的认证挑战，并使用挑战令牌返回 AES-GCM-TOKEN 解锁/驾驶级认证。
- VIN-free 命令会话与被动钥匙会话也改为两条独立 BLE 接收流，避免主动命令回复吞掉车辆的拉门认证挑战。
- 被动认证只响应发给本机已登记密钥的 UNLOCK/DRIVE 挑战，并校验 20 字节令牌与车辆会话计数器。
- CoreBluetooth 恢复被动连接后会重新启动挑战监听器；界面仅在监听器就绪后显示被动钥匙在线。

## 2.2.0

- 将现代 Vehicle Command 与原生 Phone Key 拆分为两条独立 BLE 会话，避免命令响应与被动钥匙消息争抢同一接收流。
- 保存 VIN 后仍持续保留原生 Phone Key 会话，用于车辆侧的靠近拉门认证、离车锁车和钥匙丢失检测。
- Phone Key 会话使用独立 CoreBluetooth 状态恢复标识；命令连接断开不会主动销毁仍在线的被动钥匙连接。
- 主页和诊断页分别显示被动钥匙“在线”或“恢复中”，不再把车辆命令可用误报为无感钥匙已就绪。
- 删除不能建立被动钥匙身份的现代命令状态心跳，避免不必要地保持车辆唤醒。

## 2.1.2

- 被动钥匙启用时增加无实体动作的轻量 VCSEC 在线心跳，使前台钥匙会话保持为车机当前在线钥匙。
- 用户强制退出导致 CoreBluetooth 链路消失后，车辆可依据自身座舱和驾驶状态触发钥匙丢失提示；提示内容与声音仍由车机决定。
- 修复车辆已处于驾驶授权状态时，VCSEC `genericerrorAlreadyOn` 被错误显示为操作失败的问题。

## 2.1.1

- 修复进入 App 后仅建立加密链路、车机仍将蓝牙钥匙显示为灰色的问题。
- 现代协议连接和后台恢复现在会先发送无实体动作的认证唤醒，成功后才将钥匙会话标记为已连接。

## 2.1.0

- 将单车辆存储迁移为多车辆资料库，现有车钥匙自动保留。
- 支持添加、切换和分别移除多辆 Tesla。
- 支持为每辆车设置仅保存在本机的自定义名称，留空回退真实车型。
- 新增每辆车独立的 Face ID 三档保护：关闭、仅敏感操作、全部控制。
- 按产品设计要求完整移除音乐播放进度、时间估算及缺失提示，媒体卡仅保留封面、曲目信息和播放控制。
- 新增每辆车独立的主页编辑器，支持卡片拖动排序、隐藏和恢复默认布局。
- 新增内置及自定义自动化场景，动作按本地蓝牙会话顺序执行。
- 新增车辆端充电和预热预约读取、添加与删除，支持星期重复与当前位置绑定。
- 新增附近超级充电站列表，包括距离、空闲桩、总桩数和最大功率。
- 新增低电量、门窗未关、充电停止或无电力的本地去重提醒。
- 新增 Apple Watch 配套应用，通过 iPhone 中继执行常用车辆控制。
- 修复 App 从后台返回后界面沿用失效 BLE 会话、首次控制超时的问题；前台先验证并按需重建认证会话。

## 2.0.2

- 修复网易云等未返回 Tesla 进度字段时媒体进度区完全消失的问题。
- 优先显示车机真实进度；缺少字段时使用精确匹配歌曲时长进行明确标注的本地估算，否则显示原因。

## 2.0.1

- 媒体卡显示车辆返回的真实播放进度、已播放时间与总时长；不支持该字段的媒体源自动隐藏。

## 2.0.0

- 新增充电控制中心：开始/停止充电、充电上限和充电电流。
- 新增高级座舱控制：最大除霜、方向盘加热、保持空调、爱犬、露营、生化防御和过热保护。
- 新增按车辆能力开放的哨兵模式控制。
- 新增状态新鲜度、本地连接诊断与最近操作记录。
- Siri/快捷指令新增解锁、开启空调和开始充电。
- 主页使用横向功能入口承载新板块，保持首屏简洁。

## 1.9.8

- 从协议层区分只读状态请求与控制命令：状态超时不再进入无限重试循环。
- 完整刷新期间暂停媒体轮询并拒绝重复刷新，避免 BLE 请求接收器互相等待。
- 修复车机未响应时下拉刷新持续转圈的问题。

## 1.9.7

- 主页改为固定原生导航头，内容从“蓝牙车钥匙”下方独立滚动。
- 保留隐藏滚动指示条的下拉刷新体验。

## 1.9.6

- 前台每 2 秒轻量同步车机媒体状态，中控屏切歌后自动更新曲目与封面。
- 进入后台立即停止媒体轮询。

## 1.9.5

- 将大号门锁控制压缩为电量和续航后的圆形图标按钮。
- 只有车辆连接状态栏进入详情，电量和续航区域不再误触跳转。
- 网易云封面地址统一升级为 HTTPS，避免 iOS ATS 拦截。

## 1.9.4

- 自动匹配网易云专辑封面，并使用 Apple 公共目录作精确回退。
- App 回到前台时自动刷新；连接失效时尝试重建会话。
- 移除软件版本占位，只显示车机真实返回的待安装更新信息。

## 1.9.1–1.9.3

- 被动钥匙默认开启，加入 CoreBluetooth 状态恢复与断线重连。
- 媒体暂停时保留主页卡片，电量和续航加入主页。
- 修正软件更新字段语义，删除错误的“已是当前状态”判断。
