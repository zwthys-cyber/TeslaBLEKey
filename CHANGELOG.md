# 版本记录

本文件记录已发布版本的重要用户可见变化。完整提交历史以 Git 为准。

## 2.3.0

- 修复添加第二辆车失败后当前车辆标识停留在未完成配对的新车、无法立即回到原车辆的问题；新增流程现在会原子回滚并恢复原车辆连接。
- 取消添加车辆时自动恢复进入流程前的车辆与后台 Phone Key，不再留下已断开的第一辆车。
- 切换车辆时完整清空电量、门锁、温度、充电、胎压、媒体、预约和充电站等瞬时快照，杜绝短时间显示上一辆车状态。
- 移除当前车辆后立即加载并连接下一辆已保存车辆，同时重建该车的后台被动钥匙。
- 每辆车独立保存最近操作记录；旧版全局记录仅作为首次迁移兼容，不再跨车辆串台。
- 多车菜单明确标注后台无感钥匙跟随当前车辆；每辆车继续拥有独立密钥、VIN、名称、安全设置、场景与主页布局。

## 2.2.9

- 后台被动钥匙在首次识别车辆后保存 CoreBluetooth 外设标识；App 退到后台时改为提交由 iOS 托管的已知车辆待连接，不再依赖不稳定的后台名称扫描。
- 车辆重新进入蓝牙范围并完成系统连接时，App 可由 CoreBluetooth 唤醒并自动重建 Phone Key 会话与拉门挑战监听器。
- 修复 BLE 连接等待被超时任务取消后 continuation 未及时释放的问题，降低后台恢复卡住及必须强制退出 App 的概率。
- 修复首次打开“附近超级充电站”时撞上媒体轮询会静默放弃请求、页面长期停留在暂无数据的问题；现在会等待 BLE 读取槽位并提供明确的连接忙反馈。
- 区分正在查询、车辆返回空列表、缺少完整车辆身份、连接不可用和协议失败，不再把所有情况都显示成同一个空状态。
- 超充列表补充故障桩数量、车辆标记的续航范围外状态及车辆数据更新时间。
- 空闲桩显示改为“可用数量 · 总数量”，避免将 `available/total` 误读成比例。
- 查询失败时保留上一次有效列表并显示重试入口，短暂 BLE 波动不再让已有结果消失。

## 2.2.8

- 逐项核对 Tesla 官方 Vehicle Command 实现：现代协议的前后备箱、锁车、解锁、闪灯、鸣笛、驾驶授权、充电、座舱、哨兵、车窗和媒体命令统一调用完整协议方法。
- 修复 VIN-free 关闭后备箱误用旧 RKE 开启动作：改发 `ClosureMoveRequest.rearTrunk = CLOSE`，并正确等待车辆的 `CommandStatus` 完成或拒绝结果。
- 后备箱操作新增真实闭合状态轮询，区分打开、半开、正在打开、正在关闭、无法解锁和车辆未确认关闭；执行期间禁止重复点击。
- VIN-free 会话新增基础 Vehicle Status 读取，锁车、解锁、前后备箱和充电口命令后不再只靠界面乐观猜测。
- 修复预约计划星期位掩码整体偏移一天的问题，严格使用 Tesla 的周日 bit 0、周一 bit 1 至周六 bit 6 定义。
- 充电电流、充电上限和座舱温度采用车辆返回的能力范围；未充电时清理过期功率、电流和剩余时间。
- 修复最大除霜、方向盘加热、生化防御、过热保护、空调保持模式、哨兵和车窗状态未从车辆真实状态更新的问题。
- 充电、座舱、哨兵控制在车辆未连接时不可点击；开前备箱、后备箱等敏感动作纳入本机生物识别保护。
- 预约、附近充电站、整车刷新和媒体读取使用同一串行门禁，避免多个 BLE 响应读取器互相抢占。
- 新增 VIN-free 状态、关闭尾门协议字节和 Tesla 星期掩码回归测试。

## 2.2.7

- 被动钥匙监听改为连续 BLE 事件流，删除每 60 秒重建接收器造成的短暂挑战丢失窗口，并记录拉门挑战响应耗时。
- Siri/快捷指令使用纯命令控制器，不再额外创建 Phone Key 中央管理器，避免与主 App 的后台被动钥匙争抢车辆 BLE 会话。
- 前后台生命周期提升到 App 根层统一处理，不再依赖首页是否已经显示；进入后台时即使被动钥匙仍在恢复，也会释放普通命令连接。
- 普通命令连接与被动钥匙彻底分工：后台只保留带固定 restoration identifier 的 Phone Key 会话，回到前台再恢复完整车辆控制。
- 普通命令连接只允许在 App 处于前台时建立，并合并重复的前台连接请求；系统因 BLE 事件在后台拉起 App 时不会误建控制通道。

## 2.2.6

- 修复后台无感钥匙偶发必须先打开 App：应用进程启动时立即重建带固定 restoration identifier 的 Phone Key 中央管理器，不再等待首页 UI 任务。
- 前台建立命令连接时保留已恢复的 Phone Key 连接，不再关闭并重新创建被动认证通道。
- Phone Key 首次连接未完成时保留 iOS 管理的待连接中央管理器，供车辆再次进入范围后完成恢复。

## 2.2.5

- App 进入后台后释放车辆命令 BLE 会话，仅保留可由 CoreBluetooth 状态恢复的原生 Phone Key，会为拉门认证和离车锁车让出车辆 BLE 槽位；回到前台自动恢复完整控制连接。
- VIN 页面新增系统相机实时文本识别：识别 17 位 VIN 后自动填写，手动输入仍可作为备用；最终仍校验 VIN 与当前车辆广播身份匹配。

## 2.2.4

- 根据本地运行时间线修复媒体并发缺口：切换曲目后的延迟刷新与主页两秒媒体轮询现在使用同一单实例锁，不再同时读取 BLE 响应流。
- 完整车辆刷新和控制命令也会等待媒体刷新结束，四类前台 BLE 操作形成完整互斥。
- 诊断页可直接导出 MetricKit 原始系统诊断，用于区分崩溃、卡死、CPU 异常和资源终止。
- 顶部“已连接”副标题以车机累计里程替代尾门关闭提示；数据尚未返回时明确显示等待同步。

## 2.2.3

- 修复前台完整状态刷新与用户控制命令可能同时进入同一 BLE 响应流的问题；会话验证、刷新、媒体轮询和控制现在严格串行。
- 修复被动钥匙恢复期间 App 偶发被 iOS 后台终止：后台不再启动 15 秒循环重连，避免堆积未完成的 BLE continuation。
- 后台仅保留一次由 CoreBluetooth 托管的恢复连接；回到前台后复用同一连接完成钥匙会话，不再关闭后重建。
- App 进入后台时立即取消前台重试任务，降低 watchdog 与 jetsam 终止风险。
- 状态与诊断页新增隐私安全的本地运行记录，并接收 MetricKit 崩溃、卡死和资源终止诊断；不记录 VIN、位置、媒体或命令内容。
- 将被动钥匙状态从页面底部移到顶部“已连接”旁，直接显示绿色“钥匙在线”或橙色“钥匙恢复中”。

## 2.2.2

- 修复被动钥匙断线后长期停留在“正在恢复”，必须强制退出 App 才能再次无感解锁的问题。
- 后台恢复失败时保留并复用原有 CoreBluetooth restoration 会话，不再用同一恢复标识重复创建中央管理器。
- 阻止多个被动钥匙恢复任务同时建立会话；恢复前只停止失效的协议监听器，不主动销毁系统维护的 BLE 连接。

## 2.2.1

- 修复“主动点击解锁正常，但靠近拉门无法解锁”：Phone Key 现在持续监听车辆在拉动门把手时发送的认证挑战，并使用挑战令牌返回 AES-GCM-TOKEN 解锁/驾驶级认证。
- VIN-free 命令会话与被动钥匙会话也改为两条独立 BLE 接收流，避免主动命令回复吞掉车辆的拉门认证挑战。
- 被动认证只响应发给本机已登记密钥的 UNLOCK/DRIVE 挑战，并校验 20 字节令牌与车辆会话计数器。
- CoreBluetooth 恢复被动连接后会重新启动挑战监听器；界面仅在监听器就绪后显示被动钥匙在线。

## 2.2.0

- 将现代 Vehicle Command 与原生 Phone Key 拆分为两条独立 BLE 会话，避免命令响应与被动钥匙消息争抢同一接收流。
- 保存 VIN 后仍持续保留原生 Phone Key 会话，用于车辆侧的靠近拉门认证、离车锁车和钥匙丢失检测。
- Phone Key 会话使用独立 CoreBluetooth 状态恢复标识；命令连接断开不会主动销毁仍在线的被动钥匙连接。
- 主页和诊断页分别显示被动钥匙“在线”或“恢复中”，不再把车辆命令可用误报为无感钥匙已就绪。
- 删除不能建立被动钥匙身份的现代命令状态心跳，避免不必要地保持车辆唤醒。

## 2.1.2

- 被动钥匙启用时增加无实体动作的轻量 VCSEC 在线心跳，使前台钥匙会话保持为车机当前在线钥匙。
- 用户强制退出导致 CoreBluetooth 链路消失后，车辆可依据自身座舱和驾驶状态触发钥匙丢失提示；提示内容与声音仍由车机决定。
- 修复车辆已处于驾驶授权状态时，VCSEC `genericerrorAlreadyOn` 被错误显示为操作失败的问题。

## 2.1.1

- 修复进入 App 后仅建立加密链路、车机仍将蓝牙钥匙显示为灰色的问题。
- 现代协议连接和后台恢复现在会先发送无实体动作的认证唤醒，成功后才将钥匙会话标记为已连接。

## 2.1.0

- 将单车辆存储迁移为多车辆资料库，现有车钥匙自动保留。
- 支持添加、切换和分别移除多辆 Tesla。
- 支持为每辆车设置仅保存在本机的自定义名称，留空回退真实车型。
- 新增每辆车独立的 Face ID 三档保护：关闭、仅敏感操作、全部控制。
- 按产品设计要求完整移除音乐播放进度、时间估算及缺失提示，媒体卡仅保留封面、曲目信息和播放控制。
- 新增每辆车独立的主页编辑器，支持卡片拖动排序、隐藏和恢复默认布局。
- 新增内置及自定义自动化场景，动作按本地蓝牙会话顺序执行。
- 新增车辆端充电和预热预约读取、添加与删除，支持星期重复与当前位置绑定。
- 新增附近超级充电站列表，包括距离、空闲桩、总桩数和最大功率。
- 新增低电量、门窗未关、充电停止或无电力的本地去重提醒。
- 新增 Apple Watch 配套应用，通过 iPhone 中继执行常用车辆控制。
- 修复 App 从后台返回后界面沿用失效 BLE 会话、首次控制超时的问题；前台先验证并按需重建认证会话。

## 2.0.2

- 修复网易云等未返回 Tesla 进度字段时媒体进度区完全消失的问题。
- 优先显示车机真实进度；缺少字段时使用精确匹配歌曲时长进行明确标注的本地估算，否则显示原因。

## 2.0.1

- 媒体卡显示车辆返回的真实播放进度、已播放时间与总时长；不支持该字段的媒体源自动隐藏。

## 2.0.0

- 新增充电控制中心：开始/停止充电、充电上限和充电电流。
- 新增高级座舱控制：最大除霜、方向盘加热、保持空调、爱犬、露营、生化防御和过热保护。
- 新增按车辆能力开放的哨兵模式控制。
- 新增状态新鲜度、本地连接诊断与最近操作记录。
- Siri/快捷指令新增解锁、开启空调和开始充电。
- 主页使用横向功能入口承载新板块，保持首屏简洁。

## 1.9.8

- 从协议层区分只读状态请求与控制命令：状态超时不再进入无限重试循环。
- 完整刷新期间暂停媒体轮询并拒绝重复刷新，避免 BLE 请求接收器互相等待。
- 修复车机未响应时下拉刷新持续转圈的问题。

## 1.9.7

- 主页改为固定原生导航头，内容从“蓝牙车钥匙”下方独立滚动。
- 保留隐藏滚动指示条的下拉刷新体验。

## 1.9.6

- 前台每 2 秒轻量同步车机媒体状态，中控屏切歌后自动更新曲目与封面。
- 进入后台立即停止媒体轮询。

## 1.9.5

- 将大号门锁控制压缩为电量和续航后的圆形图标按钮。
- 只有车辆连接状态栏进入详情，电量和续航区域不再误触跳转。
- 网易云封面地址统一升级为 HTTPS，避免 iOS ATS 拦截。

## 1.9.4

- 自动匹配网易云专辑封面，并使用 Apple 公共目录作精确回退。
- App 回到前台时自动刷新；连接失效时尝试重建会话。
- 移除软件版本占位，只显示车机真实返回的待安装更新信息。

## 1.9.1–1.9.3

- 被动钥匙默认开启，加入 CoreBluetooth 状态恢复与断线重连。
- 媒体暂停时保留主页卡片，电量和续航加入主页。
- 修正软件更新字段语义，删除错误的“已是当前状态”判断。
